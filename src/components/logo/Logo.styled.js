import styled from "styled-components";

export const LogoTitle = styled.h1`
  font-size: var(--heading-4-size);
  font-weight: 500;
  cursor: pointer;

  &:hover,
  &:focus {
    opacity: 0.8;
  }
`;
